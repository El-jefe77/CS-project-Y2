#OOB/ creating a class to represent a card
    #("init, special method to initilize objects or classes")
    #self are the parameters 
        #check txt file for creation of subclasses in the main code

class Card:
    def __init__(self, value, suit):
        self.value =value
        self.suit = suit 


